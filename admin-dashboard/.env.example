# API base URL (leave empty in dev — Vite proxy handles /api and /health)
VITE_API_URL=

# Control plane admin token (must match CONTROL_PLANE_TOKEN in backend .env)
VITE_API_TOKEN=change-me-admin-token
